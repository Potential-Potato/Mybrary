<body>
    <%- include('partials/header.ejs') %>
    <h2>Recently Added</h2>
    <div>
        <% bookObj.forEach(book => { %>
            <a href="/books/<%= book.id %>">
           <img height="150" width="150" src="<%= book.coverImagePath %>">
            </a>
        <% }) %>
    </div>
</body>
